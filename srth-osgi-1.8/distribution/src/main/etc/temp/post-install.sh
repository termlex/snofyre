#!/bin/sh
# -- post installation scripts for clean up and configuration -- #
 open $INSTALL_PATH/docs/${sappheiros.configuration.guide.name}
#open docs/${sappheiros.configuration.guide.name}
# java -jar ${app.configurator.name}.jar ../docs/${sappheiros.configuration.guide.name}
